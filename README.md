# Централизованная система управления скриптами для работы с Яндекс.Директ

## Описание

Эта система предоставляет централизованное управление всеми скриптами для работы с API Яндекс.Директ и Wordstat. Все скрипты используют общие модули для работы с базой данных и API, что обеспечивает единообразие и упрощает поддержку.

**🆕 НОВОЕ: Интеграция с MinIO для хранения данных**

Система теперь сохраняет все результаты в MinIO bucket вместо локальной папки, что обеспечивает:
- Централизованное хранение данных
- Масштабируемость
- Надежность
- Доступность из разных серверов

## Структура проекта

### Общие модули
- `database_manager.py` - Модуль для работы с базой данных
- `api_client.py` - Модуль для работы с API Яндекс.Директ и Wordstat
- `minio_client.py` - Модуль для работы с MinIO (хранение данных)

### Главный файл
- `main_processor.py` - Централизованный процессор, запускающий все скрипты по очереди

### Рефакторенные скрипты
- `get_campaign_ads_refactored.py` - Получение объявлений по кампаниям
- `get_extensions_and_sitelinks_refactored.py` - Получение расширений и быстрых ссылок
- `get_image_hashes_from_report_refactored.py` - Получение хешей изображений
- `get_keywords_traffic_forecast_refactored.py` - Получение прогнозов трафика
- `get_wordstat_data_refactored.py` - Обработка данных Wordstat

### Оригинальные скрипты (для справки)
- `Скрипт для получения данных о кампаниях/get_campaign_ads.py`
- `Скрипт для получения данных о кампаниях/get_extensions_and_sitelinks.py`
- `Скрипт для получения данных о кампаниях/get_image_hashes_from_report.py`
- `Скрипт для получения данных о кампаниях/get_keywords_traffic_forecast.py`
- `Скрипт для получения данных о кампаниях/get_wordstat_data.py`

## Порядок выполнения скриптов

1. **get_campaign_ads** - Получение объявлений по кампаниям
2. **get_extensions_and_sitelinks** - Получение расширений и быстрых ссылок
3. **get_image_hashes_from_report** - Получение хешей изображений
4. **get_keywords_traffic_forecast** - Получение прогнозов трафика
5. **get_wordstat_data** - Обработка данных Wordstat

## Использование

### Централизованный запуск (рекомендуется)

```bash
python main_processor.py
```

Этот файл запустит все скрипты по очереди в правильном порядке.

### Индивидуальный запуск скриптов

Каждый рефакторенный скрипт можно запускать отдельно:

```bash
# Получение объявлений
python get_campaign_ads_refactored.py

# Получение расширений и быстрых ссылок
python get_extensions_and_sitelinks_refactored.py

# Получение хешей изображений
python get_image_hashes_from_report_refactored.py

# Получение прогнозов трафика
python get_keywords_traffic_forecast_refactored.py

# Обработка Wordstat данных
python get_wordstat_data_refactored.py
```

## Настройка

### Переменные окружения

Убедитесь, что файл `.env` содержит необходимые переменные:

```env
# Настройки базы данных
DB_HOST=your_database_host
DB_PORT=your_database_port
DB_NAME=your_database_name
DB_USER=your_database_user
DB_PASSWORD=your_database_password

# Настройки MinIO
S3_ENDPOINT_URL=minio.upk-mos.ru
S3_OUTER_ENDPOINT_URL=https://www.upk-mos.ru/minio
S3_ACCESS_KEY=svitekas
S3_SECRET_KEY=dfdfdf
S3_BUCKET_NAME=dit-services-dev
S3_SECURE=False
```

### Установка зависимостей

```bash
pip install -r requirements.txt
```

### Тестирование MinIO интеграции

```bash
python test_minio_integration.py
```

### Структура базы данных

Система работает с следующими таблицами:
- `gen_report_context_contracts.yandexdirectaccounts` - Аккаунты Яндекс.Директ
- `gen_report_context_contracts.wordstatapiaccounts` - Аккаунты Wordstat API
- `gen_report_context_contracts.reports` - Отчеты для обработки
- `gen_report_context_contracts.requests` - Заявки
- `gen_report_context_contracts.contracts` - Договоры
- `gen_report_context_contracts.wordstatkeyphrases` - Ключевые фразы Wordstat

## Особенности

### Общие модули

**DatabaseManager** (`database_manager.py`):
- Подключение к базе данных
- Получение аккаунтов Яндекс.Директ и Wordstat
- Получение отчетов, заявок и договоров
- Извлечение ID кампаний из JSON
- Загрузка ключевых фраз из файлов

**API клиенты** (`api_client.py`):
- `DirectAPIClient` - для работы с API Яндекс.Директ
- `WordstatAPIClient` - для работы с Wordstat API

### Централизованное управление

**MainProcessor** (`main_processor.py`):
- Автоматически находит и использует последний файл `ads_report` из MinIO
- Передает данные между скриптами
- Обрабатывает ошибки и продолжает выполнение
- Сохраняет результаты в MinIO bucket

**MinIOClient** (`minio_client.py`):
- Подключение к MinIO серверу
- Загрузка JSON данных в bucket
- Организация файлов по папкам отчетов
- Получение списка объектов и загрузка данных

### Рефакторенные скрипты

Все рефакторенные скрипты:
- Используют общие модули `database_manager` и `api_client`
- Имеют упрощенную структуру
- Легче поддерживать и модифицировать
- Сохраняют ту же функциональность, что и оригинальные

## Результаты

Все результаты сохраняются в MinIO bucket `dit-services-dev` в папке `gen_report_context_contracts/data_yandex_direct/`:
- `{report_id}_результаты/ads_report_{report_id}.json` - Данные объявлений
- `{report_id}_результаты/sitelinks_{report_id}.json` - Данные быстрых ссылок
- `{report_id}_результаты/extensions_{report_id}.json` - Данные расширений
- `{report_id}_результаты/image_hashes_report_{report_id}.json` - Данные изображений
- `{report_id}_результаты/keywords_traffic_forecast_{report_id}.json` - Данные ключевых фраз

### Структура хранения в MinIO

```
dit-services-dev/
└── gen_report_context_contracts/
    └── data_yandex_direct/
        ├── 1_результаты/
        │   ├── ads_report_1.json
        │   ├── sitelinks_1.json
        │   ├── extensions_1.json
        │   ├── image_hashes_report_1.json
        │   └── keywords_traffic_forecast_1.json
        ├── 2_результаты/
        │   ├── ads_report_2.json
        │   └── ...
        └── ...
```

## Логирование

Все скрипты выводят подробную информацию о процессе выполнения:
- ✅ Успешные операции
- ❌ Ошибки
- ⚠️ Предупреждения
- 📊 Статистика
- 🔍 Отладочная информация

## Поддержка

При возникновении проблем проверьте:
1. Настройки подключения к базе данных
2. Настройки подключения к MinIO
3. Наличие токенов API в соответствующих таблицах
4. Права доступа к MinIO bucket
5. Корректность данных в таблицах БД

### Диагностика MinIO

```bash
# Проверка подключения к MinIO
python test_minio_integration.py

# Проверка переменных окружения
python -c "from dotenv import load_dotenv; import os; load_dotenv('.env'); print('S3_ENDPOINT_URL:', os.getenv('S3_ENDPOINT_URL'))"
```
